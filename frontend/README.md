# NeNe Suite frontend

Apex shell and installer wizard UI. **All user-visible strings** live in
`src/shared/i18n/messages/` — see [`docs/development/i18n.md`](../docs/development/i18n.md).

```bash
npm install
npm run check
```

Phase 1+ adds Vite, ESLint, and full app routes per `docs/development/frontend-standards.md`.

# Daily Report — 2026-07-02

## Summary

Closed out the **10-persona UX-remediation epic (#327)**: the four remaining high-severity
B-group findings each landed as an issue-scoped PR (#330 / #332 / #333 / #334 all closed),
then the medium backlog was swept in cheapest-first order — one PR per item — leaving only
one explicitly deferred entry (server-side org-list pagination, which needs a list-API
extension). Living docs were freshness-updated to the verified cross-repo state: the NENE2
generic TOTP primitive (NENE2#1427) is **shipped in NENE2 v1.5.333**, and installed-version
sibling adoption (invoice#496 / clear#182 / records#586) is still open on every sibling, so
live installs keep reporting `unknown`. Gates grew PHPUnit 468 → **476** and vitest 98 →
**134**, all green.

## Shipped (merged to `main`)

**Persona-eval B group (high) — complete**

- **#332 / PR #349** — a11y: shared `useFocusTrap` (initial focus, Tab/Shift+Tab cycle,
  trigger restore) applied to Modal / Drawer / CommandPalette; palette upgraded to the
  WAI-ARIA APG combobox pattern (`aria-activedescendant`, managed-focus options,
  `role="status"` empty state); AccountMenu got the menu-button pattern (arrow cycling,
  Escape/Tab restore); NotificationsMenu popover trapped + restored; install-wizard stepper
  gained `aria-current="step"` + a visually-hidden live region, and the `<ol>`'s decorative
  connector spans became pseudo-elements (valid list semantics).
- **#330 / PR #350** — help i18n: every non-ja locale now sees an explicit "help articles
  are Japanese-only, English in preparation" notice on all `/help` routes (ADR 0024's
  Japanese-first body stays); all help UI chrome (sidebar, task shortcuts, glossary chrome,
  not-found, guide-group / glossary-category headings, Callout SR prefixes) moved into the
  message catalogs per ADR 0009 / ADR 0024 §3 (`suite.help.*` +32 keys, en/ja parity).
- **#333 / PR #351** — org disable reversibility: new
  `POST /api/v1/organizations/{id}/enable` (superadmin-only, idempotent,
  `organization.enabled` audited with before/after — a full mirror of disable); the console's
  disable confirm now spells out the impact (reversible freeze, not deletion; members locked
  out; data kept; re-enable anytime; audited), and disabled rows swap the dead Disable entry
  for a working **Re-enable** flow with its own confirm. OpenAPI 31 → 32 operations.
- **#334 / PR #352** — locale-toggle clamp: the header toggle now clamps into the maintained
  en+ja pair (stub locales go to **en**, never implicitly to ja); the six-locale
  `LocaleSwitcher` (previously dead code) is mounted at Settings → General → Language as the
  one picker where every locale is reachable and escapable; the clamp spec is documented in
  `docs/development/i18n.md`; `resolveLocale` gained zh / zh-CN / zh-SG → zh-Hans aliases
  (zh-TW / zh-Hant keep the en fallback).

**Freshness (PR #353)**

- current.md / README / roadmap updated to the verified 2026-07-02 cross-repo state:
  NENE2#1427 **shipped** (commit `1984dd9` confirmed in tag `v1.5.333`; Suite-side IdP
  enforce + step-up claim remains), sibling version-probe adoption still open everywhere,
  B-group completion reflected in the README status.

**Epic #327 medium sweep — one PR per item**

- **PR #354** — memberships console: **Target organization** context banner (name + slug +
  Disabled badge) so the operator can tell which tenant they are editing; reuses the cached
  superadmin org-list query.
- **PR #355** — zh-Hans font stack now leads with simplified-Chinese fonts (Noto Sans SC /
  PingFang SC / Microsoft YaHei) instead of rendering Han codepoints with JP glyph variants;
  Noto Sans SC added to the Google Fonts link (lazy-downloaded only when used).
- **PR #356** — org console: client-side name/slug filter (+ no-match placeholder) and a
  client-side mirror of the backend slug rule with the concrete error message (was a generic
  message after a server round-trip). Server-side pagination **deferred** (needs a list-API
  extension; recorded in the epic).
- **PR #357** — install wizard: Back buttons on database / disclaimer / review (wires the
  existing `goToStep`; the server session has no per-step gate, so back-navigation stays
  consistent), selection prefilled when returning to the apps step, and the AppSelection /
  Database steps are now real `<form>`s so Enter submits from text fields.

## Epic #327 status

A group ✅ · B group (B1–B6) ✅ · medium backlog: all resolved or explicitly deferred
(server-side org-list pagination only). The epic checklist is updated in place with PR
references; the epic itself stays open for hide to close.

## Gate state

PHPUnit **476** (468 → +8) · vitest **134** (98 → +36) · PHPStan / CS / terminology / links /
OpenAPI all green. CI green on every PR (#349–#357); each merged via squash with `main` kept
in sync.

## Open questions / next

- **#341** (MFA placement question): answered in-thread; proposing closure (decision holder:
  hide).
- **O6 (epic #251)**: prerequisites all landed; next step is the ADR 0019 OQ1/OQ2 amendment
  draft before any implementation.
- Sibling-side wiring (installed-version + preflight adoption) remains the critical path for
  live version data; tracked in the sibling repos.

Last updated: 2026-07-02

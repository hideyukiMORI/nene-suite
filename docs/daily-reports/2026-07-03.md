# Daily Report — 2026-07-03

## Summary

Closed the decision loop from 2026-07-02 and opened the O6 arc. **ADR 0019 is accepted**
(PR #359 — with a review fix making the deploy capability an *opt-in flag, default off,
available to every edition*, not edition-gated). The MFA placement question (#341) was
closed as answered. Cross-repo, **nene-clear adopted both machine-surface contracts**
(clear#182/#183 → clear PR#240/#241): its `/machine/health` now reports the installed
version behind `NENE2_MACHINE_API_KEY`, and its candidate-database preflight is live with
an identity-marker migration and an env candidate allowlist. Suite docs were freshened
accordingly (PR #360), and the O6 orchestrator was sliced into implementation issues
**#361–#364** on epic #251. A session handover was added (#365).

## Shipped (merged)

- **PR #359 — ADR 0019 amendment accepted** (proposed → accepted): OQ1 = host-side deploy
  agent (no Docker socket in Suite; allow-list; audited; **opt-in capability flag, default
  off, all editions** — wording fixed in review so OSS self-host is unambiguous); OQ2 =
  staged image provenance (catalog digest pin + `/machine/health` post-recreate verification
  now; Origin-signed digests later, never blocking O6); OQ3 = Tier A coexistence explicitly
  deferred until the family installer toolkit lands.
- **#341 closed** — MFA placement answered by ADR 0025; the NENE2 TOTP primitive
  (NENE2#1427) is shipped in v1.5.333; standalone sibling MFA proceeds independently.
- **Cross-repo: nene-clear PR#240 (clear#182)** — repo `VERSION` injected as `appVersion`
  into both `RuntimeApplicationFactory` sites; `NENE2_MACHINE_API_KEY` gates the machine
  surface; public `/health` stays version-free. 4 new HTTP tests.
- **Cross-repo: nene-clear PR#241 (clear#183)** — candidate-database preflight adopted:
  inspector wired with the app's Phinx versions, the **`phinxlog`** ledger name (the
  inspector's `phinx_log` default would never recognize the schema), and the app identity
  (`nene-clear`, single-tenant DB lineage); identity marker stamped/backfilled by migration
  `20260703000000` (plain Phinx ops — the marker class opens its own transaction, which
  conflicts with Phinx's); candidates resolved only from the `NENE_CLEAR_DB_CANDIDATE_*`
  env allowlist. 10 new tests incl. "migrated own DB → `compatible` + identity `match`";
  clear backend suite 352 green, postgres-migrate CI green.
- **PR #360 — freshness**: clear adoption reflected in roadmap / current (Suite reports
  real versions for clear once the machine key is paired; invoice/records stay `unknown`).
- **O6 sliced**: issues **#361** (deploy-control seam + audit) → **#362** (dependency-ordered
  plan + min-version gating) → **#363** (halt-don't-unwind execution) → **#364** (apex
  "update all" UI); epic #251 carries the status comment.
- **#365 — this handover + report**:
  [`docs/handover/2026-07-03-ux-remediation-adr0019-o6-ready.md`](../handover/2026-07-03-ux-remediation-adr0019-o6-ready.md).

## Gate state

Suite: PHPUnit **476** / vitest **134**, all green (unchanged today — Suite-side changes were
docs). nene-clear: backend **352** (338 → +14 across the two adoptions), PHPStan L8, CS,
e2e, postgres-migrate, release-zip all green.

## Next

Implementation starts at **#361** (S2-1a deploy-control seam). See the handover §8 for the
full ordered TODO.

Last updated: 2026-07-03

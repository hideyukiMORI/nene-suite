# Daily Report — 2026-06-23

> Backfilled 2026-06-25 from the merged PR history.

## Summary

Infrastructure + docs day. Added **PostgreSQL support** to the control DB and provisioning
(ADR 0016), **automated the table-definition document** from `database/schema`, finalized the
**Origin consumption contract as ADR 0017**, and added the **frontend IA / UI brief** that
seeds the apex-shell redesign.

## Shipped (merged to `main`)

**Control DB & provisioning**

- Schema `COMMENT`s on every control-DB table/column (PR #196).
- Local-dev consolidation: single `run-local.sh` entry point (PR #199); local secrets/backups
  gitignored (PR #200).
- **PostgreSQL support** for the control DB + provisioning — ADR 0016 (PR #202).

**Schema documentation**

- Table-definition doc auto-generated from the annotated `database/schema/*.sql` snapshots
  (PR #204).
- Added lint, an ER diagram, and domain grouping to the generated doc (PR #206).

**Contracts & design prep**

- **ADR 0017 — Origin consumption contract** finalized (PR #208).
- Frontend IA / UI-element brief (PR #210).

## Next

- Apex-shell GUI redesign (P0–P9), split-hero login, and the ADR 0017 read-model re-agreement
  (next day).
